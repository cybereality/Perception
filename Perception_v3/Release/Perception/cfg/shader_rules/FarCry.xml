<?xml version="1.0"?>
<shaderConfig>
	<rules>
		<!-- Basically, this will match all shaders, they all use constants 0-3 -->
		<rule id="1" shaderCodeFindPattern="dcl_position" constantType="MatrixR" modToApply="1" startReg="0" registerCount="4" transpose="true"/>
	</rules>

	<defaultRuleIDs>
		<ruleID id="1" />
	</defaultRuleIDs>
	
	<!-- Shadows -->
	<shaderSpecificRuleIDs shaderHash="3952270238" ObjectType="Shadows"/>
	<shaderSpecificRuleIDs shaderHash="2950422486" ObjectType="Shadows"/>

	<!-- Want a better solution than this, but these shaders won't draw in stereo for some reason, so not drawing for now -->
	<shaderSpecificRuleIDs shaderHash="3149498153" ObjectType="DoNotDraw"/>
	<shaderSpecificRuleIDs shaderHash="4206807624" ObjectType="DoNotDraw"/>

	<!-- HUD stuff - This doesn't work -->
	<!-- <shaderSpecificRuleIDs shaderHash="714304875" squishViewport="true"><ruleID id="3"/></shaderSpecificRuleIDs> -->
</shaderConfig>
