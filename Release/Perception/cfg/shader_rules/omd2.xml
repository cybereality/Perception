<?xml version="1.0"?>
<shaderConfig>
  <!-- WIP Notes: The 2D elements like the HUD and menus need to use the transpose. The models in game don't. 
      Will have to identify shaders used for 2D and create overrides -->
  <rules>
    <rule id="1" constantType="MatrixC" modToApply="1" constantName ="matMVP" transpose="false"/>
    <rule id="2" constantType="MatrixC" modToApply="1" constantName ="mvp" transpose="false"/>
  </rules>

  <defaultRuleIDs>
    <ruleID id="1" />
    <ruleID id="2" />
  </defaultRuleIDs>
</shaderConfig>
